# Local SQLite setup (default for instant zero-dependency running)
DATABASE_URL="file:./dev.db"

# Or for PostgreSQL deployment (Render, Supabase, Neon)
# DATABASE_URL="postgresql://user:password@localhost:5432/nepse_db?schema=public"

PORT=3000
ML_SERVICE_URL="http://localhost:8000"
